// Server Component — no animations, pure display.
// Shows logos / social proof signals near the top of the page.

export default function TrustBar() {
  return (
    <section id="trust" className="py-12 border-y border-slate-800 px-4">
      <div className="max-w-6xl mx-auto">
        {/* ← Paste your TrustBar / social proof section here */}
      </div>
    </section>
  );
}
